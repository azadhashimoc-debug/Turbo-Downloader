*.iml
.gradle
.kotlin
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.env
# debug.keystore is intentionally committed (see build.gradle.kts debugConfig) so every
# debug build - local or CI - is signed with the same key and installs as an update over
# the previous one instead of requiring an uninstall first.
